# nginx.conf
worker_processes 1;

events {
    worker_connections 1024;
}

http {
    upstream auth_service {
        server 127.0.0.1:5000;
        server auth-service1:8081 backup;
    }

    upstream cart_service {
        server 127.0.0.1:5001;
        server cart-service1:8082 backup;
    }

    upstream event_service {
        server 127.0.0.1:5002;
        server event-service1:8083 backup;
    }

    upstream kafka_queue_service {
        server 127.0.0.1:5003;
        server kafka-queue-service1:8084 backup;
    }

    upstream order_service {
        server 127.0.0.1:5004;
        server order-service1:8085 backup;
    }

    upstream payment_service {
        server 127.0.0.1:5005;
        server payment-service1:8086 backup;
    }

    upstream product_service {
        server 127.0.0.1:5006;
        server product-service1:8087 backup;
    }

    upstream store_service {
        server 127.0.0.1:5007;
        server store-service1:8088 backup;
    }

    upstream user_service {
        server 127.0.0.1:5008;
        server user-service1:8089 backup;
    }

    server {
        listen 80;

        # Routes for microservices
        location /auth-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://auth_service/;

        }

        location /cart-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://cart_service/;

        }

        location /event-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://event_service/;

        }

        location /kafka-queue-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://kafka_queue_service/;

        }

        location /order-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://order_service/;

        }

        location /payment-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://payment_service/;

        }

        location /product-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://product_service/;

        }

        location /store-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://store_service/;

        }

        location /user-service/ {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_http_version 1.1;
            proxy_set_header Connection "keep-alive";
            # Handle CORS (optional, for development)
            add_header Access-Control-Allow-Origin * always;
            add_header Access-Control-Allow-Methods 'GET,HEAD,PUT,PATCH,POST,DELETE' always;
            add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
            if ($request_method = OPTIONS) {
                return 204;
            }

            proxy_pass http://user_service/;

        }
    }
}
